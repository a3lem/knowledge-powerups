This is the memory of the agent you are running as, compiled for this task:
system/ inlined in full below, reference/ as an index -- read a file (under
the literal root attribute above) when its description is relevant. Treat it
as read-only context: leave memory maintenance to the main session.
