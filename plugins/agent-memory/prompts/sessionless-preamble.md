This is your memory, compiled read-only from main: the session layer is
off (MEMORY_SESSION is set empty), so this session has no branch and no
worktree. system/ is inlined in full below, reference/ as an index -- read
a file (under the literal root attribute above) when its description is
relevant. Do not write memory this session; writes belong to sessions with
a branch.
