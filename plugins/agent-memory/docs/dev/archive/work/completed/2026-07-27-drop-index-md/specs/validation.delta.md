## REPLACE

### OLD

- A `system/` file without a `description` in its frontmatter is a violation.

### NEW

- Any memory file without a `description` in its frontmatter is a violation:
  for `reference/` files the description is the only thing that makes them
  findable in the injected index.
